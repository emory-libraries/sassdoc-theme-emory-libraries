{{#if (gt item.author.length 0)}}

  <h3 class="item__sub-heading">
    Author{{#if (gt item.author.length 1)}}s{{/if}}
  </h3>

  <ul class="list-unstyled">
    {{#each item.author}}
      <li>{{{.}}}</li>
    {{/each}}
  </ul>

{{/if}}